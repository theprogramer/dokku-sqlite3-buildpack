#!/usr/bin/env bash

echo "-----> Injecting sqlite3 ..."
wget https://github.com/theprogramer/dokku-sqlite3-buildpack/raw/master/vendor/shell.c -P /usr/include
wget https://github.com/theprogramer/dokku-sqlite3-buildpack/raw/master/vendor/sqlite3.c -P /usr/include
wget https://github.com/theprogramer/dokku-sqlite3-buildpack/raw/master/vendor/sqlite3ext.h -P /usr/include
wget https://github.com/theprogramer/dokku-sqlite3-buildpack/raw/master/vendor/sqlite3.h -P /usr/include

wget https://github.com/theprogramer/dokku-sqlite3-buildpack/raw/master/vendor/sqldiff -P /usr/bin
wget https://github.com/theprogramer/dokku-sqlite3-buildpack/raw/master/vendor/sqlite3 -P /usr/bin
wget https://github.com/theprogramer/dokku-sqlite3-buildpack/raw/master/vendor/sqlite3_analyzer -P /usr/bin
